DATABASE_URL="postgresql://vivola:vivola@localhost:5432/vivola"
TEST_DATABASE_URL="postgresql://vivola:vivola@localhost:5432/vivola_test"
SESSION_SECRET="change-me-to-a-random-32-character-or-longer-secret"
# Canonical origin used to build join URLs/QR codes. Required in any
# deployment behind a proxy — without it, the request's Host header is
# trusted instead, which a client can spoof. Optional for local dev.
APP_BASE_URL="https://your-production-domain.example"
